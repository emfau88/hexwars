# HEXFRONT Tactics (legacy)

This folder preserves the former, turn-based tactics prototype exactly as a historical product. It is intentionally excluded from the current HEXFRONT build, runtime, saves, and test suite.

It can be opened locally through `legacy/tactics/index.html` with a static server. It must not import from the root HEXFRONT app, and the root HEXFRONT app must not import from this folder.
