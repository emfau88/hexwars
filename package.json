{
  "name": "hexwars",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A compact turn-based hex tactics game for the browser.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
